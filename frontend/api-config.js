// Base URL of the deployed community item database Worker (see worker/README.md).
// Leave blank until you deploy it — the Item Database tab will show a
// "not configured" message instead of failing silently.
window.EQL_API_BASE = "https://eql-item-db.jbenga-iii.workers.dev";
