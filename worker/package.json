{
  "name": "eql-item-db-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute eql-item-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute eql-item-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
